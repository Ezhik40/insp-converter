name: insta360_sbs_converterer
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1  # Добавили плагин для открытия приложений
  ffmpeg_kit_flutter_new_min_gpl: ^2.6.1
  path_provider: ^2.1.3
  path: ^1.9.0

flutter:
  uses-material-design: true
