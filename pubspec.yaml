name: insp_converter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffmpeg_kit_flutter_new_min_gpl: ^2.6.1  # Самый чистый и стабильный форк с поддержкой v360
  file_picker: ^8.1.4
  path: ^1.9.0

flutter:
  uses-material-design: true
