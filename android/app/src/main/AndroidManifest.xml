<manifest xmlns:android="http://android.com" package="com.ezhik.insta360_sbs_converter">
    <!-- Эти строки намертво прописывают приложению право быть в списке All Files Access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>

    <application
        android:label="INSP 180 Converter"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name="com.example.insp_converter.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
